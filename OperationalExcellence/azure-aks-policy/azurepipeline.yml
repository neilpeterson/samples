trigger:
  branches:
    include:
      - arm-template-validation
  paths:
    include:
      - OperationalExcellence/azure-aks-policy/*

variables:
- name: resource-group
  value: aks-policy 
- name: location
  value: eastus

pool:
  vmImage: 'ubuntu-latest'

stages:

# - stage: test

#   jobs:
#   - job: tests
#     pool: Hosted Ubuntu 1604
#     continueOnError: false
#     timeoutInMinutes: 20

#     steps:

#     Temp test, replace this with ARM TTK / Python Test Suite?
#     - task: PowerShell@2
#       displayName: Install Pester
#       inputs:
#         targetType: 'inline'
#         script: |
#           Find-Module pester | Install-Module -Force

- stage: deploy
  # dependsOn: build

  jobs:
  - job: arm
    pool: Hosted Ubuntu 1604
    continueOnError: false

    steps:    
    
    - task: AzureCLI@2
      # Create resource group
      inputs:
        azureSubscription: 'azure-service-connection'
        scriptType: 'shell'
        scriptLocation: 'inlineScript'
        inlineScript: 'az group create test-2020 --location eastus'
        
    - task: AzureCLI@2
      # Create template spec
      inputs:
        azureSubscription: 'azure-service-connection'
        scriptType: 'shell'
        scriptLocation: 'inlineScript'
        inlineScript: 'az ts create --name $(spec) --version "1" \
          --resource-grouo $(resource-group) --location $(location) \
          --template-file OperationalExcellence/azure-aks-policy/azuredeploy-spec.json' 
        
